{
  "name": "@subsquid/stream-store",
  "version": "1.0.0",
  "description": "Stream store for Subsquid",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "examples": "cd examples && npm run build && cp -r ./lib .examples/stream-store && docker compose up --build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@subsquid/typeorm-store": "^1.5.1",
    "@types/amqplib": "^0.10.6",
    "amqplib": "^0.10.5",
    "bullmq": "^5.34.9",
    "dotenv": "^16.4.7",
    "kafkajs": "^2.2.4",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.5.4"
  }
}
